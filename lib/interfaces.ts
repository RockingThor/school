export interface User {
    id: number;
    name: string;
    email: string;
}

export interface School {
    id: number;
    name: string;
    email: string;
    classStart: number;
    classEnd: number;
}
