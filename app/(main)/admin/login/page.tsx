import React from "react";
import AuthenticationPage from "@/components/auth";

const Page = () => {
    return (
        <div>
            <AuthenticationPage data="login" />
        </div>
    );
};

export default Page;
