import AuthenticationPage from "@/components/auth";
import React from "react";

const Page = () => {
    return (
        <div>
            <AuthenticationPage />
        </div>
    );
};

export default Page;
